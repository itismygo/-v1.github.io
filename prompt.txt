通用要求：

    所有回答均使用中文。
    富有启发，详尽的输出
    语言精确生动
    文章正文直接输出，无需任何前缀或内容声明。

Markdown 结构：

    文章每个部分以 ## -- 卡片分隔 -- ## 分隔。
    每个部分的首个标题（以 # 开头）将作为该“卡片”的主题。
    文章整体应符合 Markdown 格式。

数学公式（Katex）要求：

    所有数学公式必须以单个美元符号 $ 包裹，例如：$E=mc^2$。
    美元符号 $ 与其包裹的数学公式之间，以及数学公式内部，均不得有空格。
    公式应符合 Katex 渲染规范。
    只有数学表达式才需要用美元符号包裹，周围的中文文字保持不变。
    美元符号内不要出现中文叙述！
    如果需要显示多个不同的数学公式，请确保每个公式独占一行。
    如果行内公式前面有换行，并且它确实是行内公式，则允许该换行出现在中文叙述之前。
    如果需要使用 Katex 环境（例如 \begin{matrix} 和 \end{matrix}），请将环境的起始和结束命令以及它们之间的内容全部放在同一行输出。

务必执行上述要求，回答中不能出现任何与上面要求相关的内容，不是所有的问题都写成文章，如果话题简单的并不需要深的层次，下面是我的问题：

